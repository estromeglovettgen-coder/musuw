## 1. Reproduce and Lock Contracts

- [x] 1.1 Add failing backend tests for the five configurable boundaries, real typed catalogs, Free/Paid authorization, invalid policy fallback, and safe DTOs
- [x] 1.2 Add failing frontend/source tests for five real controls, no standalone Chat/Embedding/fake TTS/IDs, unified four-page structure, ZIP4 locks/selects, dropdown visibility, and dark contrast
- [x] 1.3 Capture authenticated browser reproductions for the three-row omission, clipped dropdown, inconsistent page structure, and light-surface dark-mode failures

## 2. Typed Consumer Policy

- [x] 2.1 Extend the fixed scene enum, type mapping, compatibility defaults, and ordered system-setting registry for Rerank, VLLM, and ASR while keeping Chat internal and Embedding unconfigurable
- [x] 2.2 Deepen the resolver catalog/options implementation for all native types without changing `/models` or exposing provider configuration
- [x] 2.3 Extend generic Free model enforcement and SystemAdmin Models policy controls to the valid configurable typed-default union

## 3. Real Runtime Connections

- [x] 3.1 Preserve effective search-scope classification and Wiki KnowledgeQA resolution; Lite platform answer paths always resolve the Agent (`rag`) policy regardless of retrieval scope, while RAG still invokes real Embedding and Rerank stages
- [x] 3.2 Apply authorized Agent Model (`rag` scene), Wiki, VLLM, and ASR selections at new knowledge-base setup while preserving platform-owned KB Embedding identity
- [x] 3.3 Connect authorized Rerank selection to the existing tenant retrieval configuration and platform RAG runtime
- [x] 3.4 Keep custom agents, IM, evaluation, shared-owner embedding, and unrelated internal calls outside the consumer resolver

## 4. ZIP4 Settings and Theme Translation

- [x] 4.1 Replace the Lite three-row model page with five real typed controls sourced only from scene-options and existing model data
- [x] 4.2 Mechanically translate ZIP4 CustomSelect and lock/unlock behavior, remove opacity/PRO treatment, and keep locked clicks on `/plans`
- [x] 4.3 Unify General, Usage, Models, and User Profile headers, rows, spacing, responsive layout, and control geometry
- [x] 4.4 Translate complete light/dark settings, selector, chat picker, and UserMenu states through Musuw theme tokens and remove clipping/late light-only overrides
- [x] 4.5 Preserve existing language, theme, brand color, entitlement, storage, billing portal, user, tenant, logout, and Lite no-BYOK behavior

## 5. Verification and Local Handoff

- [ ] 5.1 Run strict OpenSpec, focused backend/frontend tests, relevant race tests, and authenticated browser acceptance in both themes
- [ ] 5.2 Run complete frontend/storefront/auth tests, typecheck, i18n checks, builds, complete Go tests/native build, migrations, docreader, and repository validators
- [ ] 5.3 Perform one bounded adversarial review and fix only current blockers
- [ ] 5.4 Leave the coherent change local with the existing development service running; do not commit, push, publish, or deploy
