# Musuw 主任务交接（历史文件，2026-08-22）

> **历史边界（不可作为现状或发布依据）：** 本文件记录的是 2026-08-22 的交接
> 快照。其早期 provider 读取记录不能解释为 Paddle Live 授权；其中“production
> 使用 Paddle Sandbox / Live 未授权”的叙述是当时的状态，不能当作当前配置，也
> 不能用来批准生产发布。当前权威边界见 [`PADDLE_LIVE_READINESS.md`](PADDLE_LIVE_READINESS.md)、
> [`SECRETS_AND_INTEGRATIONS.md`](SECRETS_AND_INTEGRATIONS.md) 和
> [`STAGING_OPERATIONS.md`](STAGING_OPERATIONS.md)：production 的目标/合同是完整
> Paddle Live，`staging.musuw.com` 才使用独立 Sandbox；两者不得混用。

不要从本文件恢复旧的运行时、凭据、Paddle 配置、域名、数据或发布流程。任何
当前执行都必须重新核对代码、OpenSpec、GitHub Environment 和 provider Dashboard，
并遵循 staging-only → 完整 Sandbox E2E → 同 SHA/digest promote 的发布门。

## 接管目标

运营中台已经收口。新执行任务只完成剩余交付，不再重做或横向扩展运营台。
始终以 `/Users/yangdi/Desktop/任务列表.txt` 为五项任务权威清单，遵守其中的
奥卡姆剃刀、禁止子智能体、不中途停下和真实浏览器验收要求。

仓库：`/Users/yangdi/Documents/Codex/2026-08-16/musuw-five-tasks/musuw`

源任务 ID（仅审计）：`01a02398-d492-7483-b3aa-29eb34b0f5ab`。本交接已包含
继续执行所需上下文；不要依赖旧任务的浏览器或 REPL 状态。

接管后按顺序完整读取：

1. `/Users/yangdi/Desktop/任务列表.txt`
2. 本文件
3. `docs/SECRETS_AND_INTEGRATIONS.md`
4. `docs/DEPLOYMENT.md`
5. 下列三个仍有未勾选项的 OpenSpec `tasks.md`

## 历史快照：运营中台（不可作为当前状态）

- Appsmith 已退出运行链路。固定左栏的 TDesign Vue Next 中台包含概览、用户、
  知识库与文档、账单、身份、存储、日志与追踪七页。
- 交接时的 PRODUCTION 快照：WeKnora、Supabase Auth Admin、Cloudflare R2、Langfuse
  和当时选定的 Paddle provider 读取适配器有真实连接证据。Paddle 读取能力或本机
  credential 存在性不构成 Live 授权；当时产品运行时固定为 Sandbox。该 Sandbox-
  production 叙述已过时，绝不能当作当前配置或 Live 授权证据。
  页面实际数据与对象/trace 计量属于运营台历史验收，不改变支付环境边界。
- 交接时的 TEST 快照：Paddle Sandbox、Supabase、Langfuse 连接；R2 按真实架构显示
  TEST 本地存储、不适用，不伪造凭据或空清单。当前 staging 以
  [`STAGING_OPERATIONS.md`](STAGING_OPERATIONS.md) 为准。
- 所有业务写操作只走 WeKnora capability-scoped API；用户管理要求
  `UPDATE:<tenant_id>`，队列保留确认框。settings/API-key management 仍 404，
  未确认 mutation 403，调查投影不含 prompt/content/attachments/keys/payload。
- `npm run admin:test`、`npm run admin:build`、`npm run admin:e2e`、生产静态
  preflight 与 OpenSpec strict 均已通过。七页 WCAG serious/critical 为 0。
- 密钥位置和当前存在性见 `docs/SECRETS_AND_INTEGRATIONS.md`。不要把密钥
  改为明文 env，也不要用会显示值的 Keychain、DOM、日志或 SSH 命令。

本地运营台入口保持 loopback-only：

```sh
MUSUW_ADMIN_PRODUCTION_UNLOCK=I_UNDERSTAND_THIS_IS_LIVE \
  scripts/musuw-admin production
```

入口为 `http://127.0.0.1:4186`。运营台不需要也不应该发布到公网。
解锁短语中的 `LIVE` 指访问真实生产业务数据，不代表 Paddle Live 已获批。

## 唯一剩余 OpenSpec 清单

不要另起平台或新架构。直接完成以下 6 个未勾选项：

1. `openspec/changes/production-legal-lifecycle-release/tasks.md`
   - 3.1：提交并 push 完整 main，精确 SHA 的 CI 必须 green。
   - 3.2：Cloudflare storefront 与 immutable-GHCR server release 都要 terminal
     success，并核对公网 revision/health。
   - 4.1：生产浏览器走法律/auth、注册登录、知识创建、上传解析、检索/对话、
     plans/Paddle checkout handoff、退出与重新登录；禁止新的 Live 收费。
   - 4.2：从产品 UI/API 删除一次性验收知识，记录部署 SHA 和证据。
2. `openspec/changes/openrouter-gemini-video-ingestion/tasks.md`
   - 4.2：在东京默认 VLM（不带 per-upload override）上完成一次有界
     Gemini 2.5 Flash 视频请求。
   - 4.3：部署当前完整模型目录，以付费 entitlement 在生产完成视频上传、
     ingestion、retrieval、cleanup。东京 serving origin 已恢复默认
     `builtin-openrouter-vlm` → Gemini 2.5 Flash / OpenRouter `google-vertex`；此前
     香港 Qwen 3.7 Flash 仅作历史区域平替证据，不是当前默认链路。
3. `openspec/changes/zero-config-knowledge-agent/tasks.md`
   - 4.2：同一次生产发布里验收 create/upload/retrieval、Flash、Pro、深度思考
     和工具能力，最后删除刻意创建的验收知识库。

`openspec/changes/local-operations-admin/tasks.md` 已全部完成，除非新发布造成
回归，否则不要再设计运营台。

## 当前必须一并解决的生产阻塞

### 邮箱验证码投递

用户实测 OTP 曾由 `login@auth.xhslab2607.win`、显示名 `Musnow` 发出并进入
Gmail 垃圾箱。这不是视觉问题，必须在生产生命周期前解决：

- 先通过 Supabase Production Auth 日志和官方 SMTP 设置确认真实发送链路；
  不凭猜测改代码。
- 使用成熟事务邮件供应商的自定义 SMTP 和已验证的 Musuw 发件子域，配置
  SPF、DKIM、DMARC、From 与显示名。Cloudflare 可以承载 DNS/入站转发，
  但 Cloudflare Email Routing 不是 Supabase OTP 的出站事务 SMTP。
- 不复用旧 `xhslab2607.win` 或 `Musnow` 品牌。用新 Gmail plus-address 完整
  注册，证明验证码在合理时间到达；同时检查主收件箱、垃圾箱、认证结果和
  Supabase rate limit。不得在证据里抄录 OTP 或 SMTP secret。

### 生产发布与运行状态（历史记录，需重新核验）

- SSH alias：`musuw-tokyo`；公网应用：`https://app.musuw.com`；官网：
  `https://musuw.com` / `https://www.musuw.com`。
- 当前服务器 app 已因 Langfuse 配置被安全 recreate 且 healthy；当前 release
  source 曾原位同步三份 Langfuse 运行文件。备份在
  `/opt/weknora/runtime/backups/langfuse-observability-20260823`。下一次正常
  immutable release 要让服务器回到最终提交的精确 source/image SHA，不要再
  维护第二套临时发布方法。
- 不删除命名卷、数据库、R2 对象或服务器 secret。一次性验收内容只能通过
  产品 UI/API 删除。
- 交接时 Paddle checkout、portal、provider lifecycle 和 reviewer evidence 只使用
  完整 Sandbox 单元；历史 Live-shaped provider 读取不能驱动 checkout 或部署。
  这段是历史记录，不代表当前 production。当前执行必须以 Live readiness 和
  staging Sandbox runbook 重新核对，并禁止创建新的真实收费/退款。

### 东京迁移交接记录

- 生产 serving origin 已切换到东京；运维 SSH 使用固定别名
  `musuw-tokyo` 与 pinned host key。`server-production` 仅更新了受限 remote
  与 pinned host key，deploy private key、runtime secrets 与公开构建环境未变。
- Tokyo current source、app/frontend revision labels、immutable image
  repository digests、state parity 与 loopback health 均已核验；Cloudflare
  Tunnel 只有 Tokyo connector 运行，香港 connector 停止。
- 执行包含两次短维护窗口：第一次在本地镜像 bootstrap 阻塞后验证香港回滚并
  恢复公网；第二次完成最终 sealed snapshot/restore 后切换 Tunnel。香港主机、
  source releases、命名卷、Tunnel 输入与回滚环境 bundle 均保留。
- 回滚采用停机保盘姿态：停止 Tokyo public traffic，保持香港 app/Tunnel 停止并
  保留其 runtime、命名卷、source releases 与 Tunnel 输入，由运维在独立窗口处理
  数据恢复。观察完成后香港 guest 在 active public services=0 预检通过后已
  poweroff，磁盘与卷保留。本次不执行 hot reverse-sync，也不把只切 Tunnel
  视为数据安全回滚。
- Tokyo direct SSH 已在本机 Clash DIRECT 规则下稳定；若该本地路径再次异常，
  先使用 pinned 运维通道诊断，不修改腾讯安全组或申请云 API key。

## 完成标准

- 迁移适用任务及上述 OpenSpec 项均有新鲜证据；hot reverse-sync 项按用户选择
  标记为不适用，停机保盘边界已记录。
- 邮件发件品牌、域认证和 Gmail OTP 投递真实通过。
- main 精确 SHA 的 CI、storefront 与 server release 终态成功，公网 revision
  与 health 匹配该 SHA。
- 真实 Chrome 完成正常、关键失败与清理路径；不把空数据、unavailable 或
  provider dashboard 截图当作产品链路成功。
- 工作树干净，文档按事实更新，OpenSpec strict 全通过；此后才能将唯一 goal
  标为 complete。
