#!/usr/bin/env bash
# Fast production path for visual-only changes. The deploy helper enforces the
# source allowlist again on the host before it builds/recreates frontend only.
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$repo_root/scripts/weknora-deploy.sh" update-ui "$@"
